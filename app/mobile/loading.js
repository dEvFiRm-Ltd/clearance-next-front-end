import InitialMobileHeader from '@/components/Account/mobile/initialMobileHeader';
const Loading = () => {
  return (
    <div>
      <InitialMobileHeader />
    </div>
  );
};

export default Loading;
